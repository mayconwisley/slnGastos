<UserControl x:Class="Gastos.Presentation.Views.ClientesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="12" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Border Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Nome do cliente" />
                    <TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,12" />
                    <CheckBox Content="Cliente ativo" IsChecked="{Binding Ativo}" />
                    <TextBlock Text="{Binding Status}" Margin="0,12,0,0" Foreground="{DynamicResource PrimaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="24,0,0,0">
                    <Button Content="Novo" Command="{Binding LimparCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" />
                    <Button Content="Salvar" Command="{Binding SalvarCommand}" Margin="0,0,8,0" />
                    <Button Content="Excluir" Command="{Binding ExcluirCommand}" Background="{DynamicResource NegativeBrush}" />
                </StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Clientes}" SelectedItem="{Binding ClienteSelecionado}" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*" />
                    <DataGridTextColumn Header="Login de cadastro" Binding="{Binding Login}" Width="220" />
                    <DataGridCheckBoxColumn Header="Ativo" Binding="{Binding Ativo}" Width="90" />
                    <DataGridTextColumn Header="Data de cadastro" Binding="{Binding DataCadastroUtc, StringFormat=dd/MM/yyyy}" Width="150" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
